<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="16dp"
    android:paddingBottom="10dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-black"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:text="@string/similar_apps_title"
        android:textColor="?text_primary_color"
        android:textSize="16sp" />

    <!--
        No top margin and a short bottom padding: every app card brings its
        own 12dp around the icon and 6dp under the name, which is what
        spaces the shelf off the title and off the next block.
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        tools:listitem="@layout/profile_app_item" />

</LinearLayout>
